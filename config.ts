/**
 * !Important!
 * config.tokens don't touch
 */

const config = {
    run: true,
    tokens: {
        main: "",
    },
    commands: {
        registerInGuild: false,
        guild: "",
    },
    locale: "ru",
};

export default config;
